CREATE TABLE IF NOT EXISTS levels
(
    LEVEL_ID int NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CRITERION_ID INTEGER NOT NULL,
    TITLE VARCHAR (30),
    DESCRIPTION VARCHAR (255),
    MARK INTEGER,
    IS_REMOVED boolean DEFAULT FALSE
);